# cities-data-dash
🚀 Quick Start:

Create a new GitHub repository
Create these files in your local project folder
Initialize git and push:

bashgit init
git add .
git commit -m "Initial commit: US Cities Dashboard"
git remote add origin https://github.com/yourusername/us-cities-dashboard.git
git push -u origin main

Run locally:

bashpip install -r requirements.txt
python app.py